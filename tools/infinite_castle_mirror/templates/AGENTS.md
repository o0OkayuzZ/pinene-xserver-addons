# AUTO-SYNCED MIRROR / DIRECT EDIT禁止

- This repository is NOT an independent development source. The sole authority for Infinite Castle and the server is https://github.com/o0OkayuzZ/pinene-xserver-addons/tree/main .
- Do not directly edit or develop implementation, README, this file, or CI here. Make changes to the source repository and its mirror templates; let its one-way sync publish them.
- Before Infinite Castle work, fetch both repositories' latest `main`. Inspect current configuration. Latest repository state overrides historical chats, paths, versions and commits.
- If this mirror is stale, inspect `SOURCE_COMMIT.json`, the source `sync-infinite-castle.yml` workflow and authentication. Trigger a resync; do not manually repair generated files or introduce reverse sync.
- `archive/`, `development/` and `snapshot-files.json` are historical reference material, not competing implementations. Preserve them. Do not delete branches without explicit authorization.
