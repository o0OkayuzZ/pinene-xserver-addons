# 並行作業の統合記録（2026-09-24）

基準: `2eba8eea07d4e23cc82ae39182542925eaac7f8b`。
統合ブランチ: `integrate/parallel-appraiser-pinecd-20260924`。
元の作業ツリーを変更せず、差分・未追跡ファイルを別途バックアップして取り込んだ。

## このブランチへ取り込んだもの

- 鑑定士: `pinene-appraiser-work` の未コミット実装39ファイル。自然個体の個別期限、名札による永久化、名札操作時の鑑定UI抑止、複数個体共存、自然由来の命名個体を含む死亡時の赤・茶・深紅・歪んだ各64個を保持する。
- 配信用BPとauthoring側の4ファイルを同時に取り込み、既存GFと混同しない。元の実機報告は `../mycology/APPRAISER_LIFECYCLE.md`。その11/12ケース成功・アンロード実証未合格という限界も保持する。
- PineCD: ローカルに残った512px上限チェックと4回帰テストを取り込む。チェック本体を `tools/pinecd/validate_textures.py` に分離し、既存validatorからも呼び出す。
- 現行mainのPNG・OGGは一切再生成・置換しない。旧ローカル版PNGのハッシュを現行画像へ強制しない。historical validatorの既存保全アサーションも削除・緩和しない。
- RP01を1.0.33へ更新し、過去に配備した512px画像のキャッシュ更新用versionを確保。BP15は1.0.81。依存先を含む6パックの同期値は `parallel-integration-20260924-versions.json` を参照。
- GF・Auto Repair・CrossbowのゲームロジックとBP16は変更しない。クロスボウテストの固定version期待値だけを今回のmanifest値へ同期する。

## 同じ配備予定に追加したが、まだ取り込まないもの

- BSL v3: PR #20、確認head `e26a76228034edf5d322149be1115b938492aaa0`。アイスボックスは27スタック設計だが、PR全体はDraft。通常報酬の合意済み増量の未適用、Bastionのネザライト強化型紙が確定でない問題、Special容量検証、実機受入・配備versionの見直しを解決してから統合する。
- ディメンション移動後のチェスト異常: 別作業の `full-stack-test-result.json` は原因未特定・ローカル単体プレイで再現なし。ネットワーク試験は接続できず未実施。修正コードが完成した扱いで混ぜない。
- 料理・まな板・バター等とGF追加カード／販売NPC: 今回確認できた情報は仕様・画像・構想。未確認の実装を作成済み・配備済みとは扱わない。

## 配備ゲート

この統合操作ではローカル実機ワールド・Xserver・公開Webを書き換えない。
既存mainの `pineHasTag` 未定義エラー、既存全repoテスト失敗、BSLの未解決事項は別途解消・検証が必要。
バックアップや試験用ワールド、診断用BPは本番パックへ取り込まない。

## 統合後の検証

- Mycology 109件、GF 156件、Auto Repair/PvP 51件、Crossbow 15件、Castle 115件成功。
- メタデータ・ケース資産5件、PineCD上限4件成功。18枚すべて512px以内。生成物一致、Mycology静的検証、ownership audit、差分空白検査も成功。
- `npm test`: 基準mainは419/424成功、統合後は448/453成功。同じ名前の5失敗を確認し、新規29ケースは成功。テスト期待値を削除して失敗を隠していない。
- 5件には `pineHasTag` 未定義に対応するテストも含む。単に古いテストだから無害とは扱わない。統合validatorも両方で同じ `egbread.json` の履歴ハッシュ照合に失敗する。
- 詳細は `parallel-integration-20260924-validation.json`。本番への追加配備・実機受入完了を意味しない。
- 取り込み時にCRLFをLFへ正規化した。元の作業ツリーはそのまま保持し、配信用/authoring側の一致を検証した。過去の実機ハッシュ証跡は元実行時の記録として保持する。
- 掲載用のUTF-8テストログは行末空白と末尾空行だけを整形した。元ログは元worktreeと取り込み前バックアップに保持。
