# 無限城の信頼性・録画コマンドと剣の振り動作 — 全体反映

2026-09-15、ユーザーの全体適用・mainへのpush指示に基づく反映。

## 最終変更

- 再構成の永続復旧、ready/spawn監視、敵スロット保存、退出猶予、出口床修復、宝箱保護を適用。
- `/scriptevent infinite_castle:rebuild_record start` を追加。無限城内のクリエイティブプレイヤーが攻略棟・装飾棟を全体再構成できる。城内は全員クリエイティブ／スペクテイターが条件。
- 既にローカル・本番にあったV5の報酬数、再構成間隔、集団移動、上層の敵出現修正を統合。動かせる構成がない場合は復旧記録を終了して再試行する。照明の状態判定も大文字のCOMPLETEに対応。
- 再構成の読み込みはnative promiseの完了を待ち、タイムアウト・失敗を永続復旧へ送る。到着地点の既存チャンク再試行処理は維持。
- BSLの壺テーブルからHeavy Coreだけを削除。
- パーカナイトの剣は固定ポーズを重ねる専用attachableを2つの統合RPから削除し、標準の手持ち描画へ戻した。テクスチャ・攻撃力11・耐久値は維持。
- プレイヤーOutline定義に現在のエンジンが使う`first_person_item_rotation_factor`を追加。再生成ツールにも同じ補正を追加。

変数追加後の再起動ログでは未定義変数エラーは消えていたが、ユーザーから振り動作の引っかかりが報告されたため、専用attachableの削除を追加した。削除後の実機動作は未確認。

## 反映先

| 対象 | 今回の更新ファイル | 結果 |
| --- | ---: | --- |
| XServer本番 | 29 | 接続者0人で停止、ワールドをバックアップ、更新・起動確認、起動エラー0 |
| 開発用ワールド `8v9pvwiD6QQ=` | 15 | 先行適用済みの信頼性修正に、剣の追加修正と配布バージョンを反映 |
| `IC_Phase1_Fresh_20260911` | 35 | 更新・ハッシュ照合完了 |
| 共有インストール済み／開発元パック | 58 | 古い無限城の不足スクリプトも補完、旧剣attachableを削除 |
| Aurealis | 0（共有経由） | 共有無限城を参照するため、共有側の更新で反映 |

全対象で変更前ファイルを保存し、削除対象を含むSHA-256照合を実施。ローカルはセーブDBを操作せずパックだけを更新した。共有パックは既存のUUID・依存バージョンを維持。配布用の各パックと登録情報は依存先の更新に合わせて同期した。Zombie Gearの変更はmanifestのバージョン・依存バージョンのみ。

本番バックアップ: `/opt/minecraft/server/_pinene_deploy_backups/ic_reliability_20260915_220809`。
ローカルバックアップ: `C:/work/ic-global-release/backups/`。

## 検証

- `npm test`: 272件通過（無限城110件、Pinenite61件を含む）。
- 変更JavaScript 35ファイルの構文検査、変更JSON、依存先と登録バージョン、配布ハッシュを検査。
- 本番サービスactive、Server started確認、起動ログ／Content Logのerror・exceptionは0。
- 旧`tools/validate.py`は過去のMycologyリリースのハッシュ固定検査で失敗。最初の対象は今回未変更の`egbread.json`（チェックアウト時の改行差）。現在の全体リリース検査には利用できないため、失敗を記録した上で現在の自動テストと配備事前検査を実施。
- クライアントでの剣の振り動作、2〜4人での復旧、録画コマンド実行は未確認。Minecraftを完全終了して再起動する必要がある。

配備結果・起動ログは[証跡フォルダ](global-rollout-evidence/)に保存。
