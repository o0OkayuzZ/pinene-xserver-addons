# 城・城飾りの待ち時間を釣鐘型の抽選へ — 2026-09-12

無限城BP 0.2.5。城本体は5〜15分、城飾りは15秒〜2分の範囲で、中央が出やすい正規分布から次回の待ち時間を抽選する。最新・開発用ワールドへ反映済み。保存して入り直すと適用される。Xserverは接続者1人のため未適用。ユーザー指定に従い、接続者0人になったら自動反映する待機ジョブを登録した。

| 対象 | 最短 | 最長 | 平均・最頻付近 | 元の正規分布の標準偏差 |
|---|---:|---:|---:|---:|
| 城本体 | 5分 | 15分 | 10分 | 1分40秒 |
| 城飾り | 15秒 | 2分 | 67.5秒 | 17.5秒 |

範囲の両端を平均±3σとする。Box-Muller法で正規乱数を作り、範囲外は引き直す切断正規分布。端へ丸めて極端な待ち時間を増やす処理は使わない。最後にゲームの1tick（0.05秒）単位へ丸める。切断後の分布の標準偏差は元よりわずかに小さくなる。

約68%は本体8分20秒〜11分40秒、城飾り50〜85秒に収まる。均等抽選や毎回固定のタイマーではない。

## タイマーと保護

- 初回、各再編成の完了後、本体再編成後に再予約する城飾りにも同じ抽選を使う。従来の初回10秒・固定150秒・固定15分は使わない。
- チェックするたびに引き直さず、抽選済みの期限を保存して再入場・再起動後も継続する。0.2.5の初回だけ旧固定予約を新しいキーで作り直す。
- 昼夜から独立した経過tick時計、在室プレイヤー・戦闘・出口の保護を維持する。安全上延期する場合の20秒再試行は新しい通常周期の抽選とは別に扱う。
- 範囲は再編成を予約する待ち時間。処理負荷・候補探索・本体優先・プレイヤー保護で実際の開始は遅れる場合がある。無人では再編成しない。
- 飾りは遠くの残響付きの琴、本体は近くの明瞭な琴という前回の音の区別を維持する。

## 内部検証

51件成功。両タイマーそれぞれ10万回の固定seed抽選で範囲・平均・標準偏差・左右対称の山型・中央±1σの頻度・端への確率集中がないことを確認した。範囲外の引き直し、乱数異常時に無限ループしない上限も確認。

実際のmanagerのタイマー関数を使い、初回から抽選すること、待機中は再抽選しないこと、完了後は1回だけ抽選すること、昼固定・再起動・保護延期・無人時の挙動を確認。戦闘・鍵・報酬・移動・部屋素材の回帰も成功。追加のクライアント操作テストは実施していない。

ローカルは変更したパック6ファイルとBP登録を反映し、SHA-256を照合。配備でセーブDB・level.dat・建築や進行の保存値は書き換えていない。変更前パックは `%USERPROFILE%/.codex/backups/ic-interval-20260912-105237/` に保存。

[分布設定・検証件数・配備結果](bell-intervals-20260912.json)。

Xserver待機ジョブ: `pinene-interval-20260912-105238.service`。約30秒ごとに人数を照会し、0人を確認した時だけ配備直前に再確認して停止・全体バックアップ・反映・再起動を実行する。元パックに別の変更が入った場合は上書きせず停止する。状態: `/tmp/pinene-interval-20260912_105238/queue-status.json`、完了結果: `/tmp/pinene-interval-20260912_105238/result.json`。
