# 墓生成の判定と復活時の状態回復

墓が作られないという報告を受けて調査。適用前の墓BP 1.0.12 / RP 1.0.9はサーバーに登録・ロードされ、Windows受信キャッシュの墓RP全73ファイルも一致した。パックの欠落は確認されなかった。報告された死亡の場所・持ち物・実行経路は未特定であり、以下の不具合がその死亡に該当したかは未確認。

死亡判定が古い`empty`タグに依存し、靴の実際のタグ`getFeet`を`getFFeet`と誤記していた。また、復活時に座標情報が欠けると`dead`タグが解除されず、以後の墓生成が抑止される。

- 現在のインベントリと装備を優先して判定し、コンポーネントがない場合だけ既存タグへフォールバック。
- 復活時に不完全な死亡タグも解除。無限城の除外、空インベントリ設定、二重生成防止は維持。
- 生成・スキップ理由・例外を`[graves]`ログへ出力。アイテム移動処理は変更なし。
- 墓BP 1.0.13 / RP 1.0.10。依存関係とワールド登録を更新。

8件の回帰テストが合格（`node --test tests/grave-policy.test.mjs`）。main.js構文検証も合格。全体の`tools/validate.py`は今回未変更の金食料`items/egbread.json`と既存ハッシュ一覧の不一致で停止。

Xserverと「開発用ワールド」`8v9pvwiD6QQ=`へ適用。変更した2スクリプトのSHA-256が両環境で一致。バックアップはサーバー`_pinene_deploy_backups/grave-fix-20260920-212118`とローカル`C:/work/nf-release/grave-dev-backup`。ワールドDBのコピー・置換は実施していない。

接続者0人で再起動。初回はワールド起動後もTCP接続受付が開始されず、再起動後21:22に外部HTTP受付を確認。起動時Content Logは0バイト。ゲーム内の死亡・墓生成・アイテム回収は未確認。

## 開発ワールドとの比較

適用前に全36パックの登録バージョンが一致、33パックは内容も一致。JSON整形とテキスト改行を正規化して比較した。墓BP・RPと独立影RPは一致。

差分は統合BPのカード機能関連15ファイル、Pinenite Model Outlinesの3ファイル、PvP Island RPのmanifest。詳細は[evidence/graves-20260920/development-comparison.json](evidence/graves-20260920/development-comparison.json)。これらは今回の墓修正では変更していない。ローカル開発ワールドが最新版というユーザー指定を以後の同期基準とする。
